{
  "name": "telegram-xui-bot",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "prestart": "python3 -m pip install --user -r requirements.txt",
    "start": "python3 src/bot.py",
    "test": "echo \"Error: no test specified\" && exit 1"
  }
}